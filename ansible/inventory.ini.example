# Federated Commons - Ansible Inventory Example
#
# NOTE: inventory.ini is auto-generated by Terraform.
# Run `terraform apply` in terraform/infrastructure/ to generate it.
#
# The generated inventory includes all servers provisioned by Terraform:
# - management (if var.create_management = true)
# - tools-prod (always)
# - authentik-prod (if var.create_authentik = true)
# - staging (if var.create_staging = true)
# - dev (localhost)
#
# Example IPs below use RFC 5737 documentation range (192.0.2.x).

[management]
192.0.2.10 ansible_user=ubuntu ansible_ssh_private_key_file=~/.ssh/id_ed25519

[tools-prod]
192.0.2.20 ansible_user=ubuntu ansible_ssh_private_key_file=~/.ssh/id_ed25519

[authentik-prod]
192.0.2.30 ansible_user=ubuntu ansible_ssh_private_key_file=~/.ssh/id_ed25519

[staging]
192.0.2.40 ansible_user=ubuntu ansible_ssh_private_key_file=~/.ssh/id_ed25519

[dev]
localhost ansible_connection=local
