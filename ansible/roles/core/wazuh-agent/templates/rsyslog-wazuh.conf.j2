# Wazuh agent reads logs directly from files configured in ossec.conf
# No syslog forwarding needed - agent monitors /var/log/* directly
# This file is kept as a placeholder to prevent legacy config issues
