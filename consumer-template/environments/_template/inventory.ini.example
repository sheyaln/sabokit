# Copy to inventory.ini. Fill IPs from `terraform output compute_hosts`.

[apps]
apps-prod ansible_host=203.0.113.10 ansible_user=ubuntu

# Add additional hosts as your compute_hosts topology grows.

[all:vars]
ansible_python_interpreter=/usr/bin/python3
