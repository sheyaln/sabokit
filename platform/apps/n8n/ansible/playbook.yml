---
# Deploy n8n. Vars supplied by the consumer's site.yml (or directly on the
# command line). All required vars are documented in the platform/apps/n8n/ README.

- name: Deploy n8n
  hosts: "{{ n8n_host_group | default('apps') }}"
  become: true
  roles:
    - role: n8n
