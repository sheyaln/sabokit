---
# Deploy Postiz. Vars supplied by the consumer's site.yml (or directly on the
# command line). All required vars are documented in the platform/apps/postiz/ README.

- name: Deploy Postiz
  hosts: "{{ postiz_host_group | default('apps') }}"
  become: true
  roles:
    - role: postiz
