---
# Intentionally empty. Multi-instance role invocation (one include_role per
# Backrest instance) makes handlers brittle: handlers fire at end-of-play with
# whichever per-instance fact value was set last, restarting the wrong
# instance. The role's tasks/main.yml does inline conditional restart instead,
# scoped to the current iteration's backrest_install_dir.
