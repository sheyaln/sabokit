---
# Deploy Steward. Vars supplied by the consumer's site.yml (or directly on the
# command line). All required vars are documented in the
# platform/apps/steward/ README.

- name: Deploy Steward
  hosts: "{{ steward_host_group | default('apps') }}"
  become: true
  roles:
    - role: steward
