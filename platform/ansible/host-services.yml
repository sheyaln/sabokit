---
# AUTO-GENERATED by scripts/gen_apps_yml.py. Do not edit by hand.
# Adding a host-service bundle: append to HOST_SERVICES_BUNDLES in the
# generator, then re-run it.
#
# Per-host runtime watchers (autoheal, future diun + wazuh-agent). One
# import_playbook block per (bundle, host) pair. Each block is guarded by
# `enabled_apps.<slug>_<host>` being non-null -- the base layer's
# host_services for_each only produces an entry for hosts the consumer
# didn't disable, so disabled hosts evaluate to null and skip.
- import_playbook: ../base/host-services/autoheal/ansible/playbook.yml
  vars:
    autoheal_host_group: "{{ enabled_apps.autoheal_tools.ansible_group }}"
    autoheal_image: "{{ enabled_apps.autoheal_tools.ansible_vars.autoheal_image }}"
    autoheal_image_tag: "{{ enabled_apps.autoheal_tools.ansible_vars.autoheal_image_tag }}"
    autoheal_container_label: "{{ enabled_apps.autoheal_tools.ansible_vars.autoheal_container_label }}"
    autoheal_interval_seconds: "{{ enabled_apps.autoheal_tools.ansible_vars.autoheal_interval_seconds }}"
    autoheal_start_period_seconds: "{{ enabled_apps.autoheal_tools.ansible_vars.autoheal_start_period_seconds }}"
    autoheal_timezone: "{{ enabled_apps.autoheal_tools.ansible_vars.autoheal_timezone }}"
  when: enabled_apps.autoheal_tools is defined and enabled_apps.autoheal_tools is not none
  tags: [host_services, autoheal, autoheal_tools]
- import_playbook: ../base/host-services/autoheal/ansible/playbook.yml
  vars:
    autoheal_host_group: "{{ enabled_apps.autoheal_identity.ansible_group }}"
    autoheal_image: "{{ enabled_apps.autoheal_identity.ansible_vars.autoheal_image }}"
    autoheal_image_tag: "{{ enabled_apps.autoheal_identity.ansible_vars.autoheal_image_tag }}"
    autoheal_container_label: "{{ enabled_apps.autoheal_identity.ansible_vars.autoheal_container_label }}"
    autoheal_interval_seconds: "{{ enabled_apps.autoheal_identity.ansible_vars.autoheal_interval_seconds }}"
    autoheal_start_period_seconds: "{{ enabled_apps.autoheal_identity.ansible_vars.autoheal_start_period_seconds }}"
    autoheal_timezone: "{{ enabled_apps.autoheal_identity.ansible_vars.autoheal_timezone }}"
  when: enabled_apps.autoheal_identity is defined and enabled_apps.autoheal_identity is not none
  tags: [host_services, autoheal, autoheal_identity]
- import_playbook: ../base/host-services/autoheal/ansible/playbook.yml
  vars:
    autoheal_host_group: "{{ enabled_apps.autoheal_management.ansible_group }}"
    autoheal_image: "{{ enabled_apps.autoheal_management.ansible_vars.autoheal_image }}"
    autoheal_image_tag: "{{ enabled_apps.autoheal_management.ansible_vars.autoheal_image_tag }}"
    autoheal_container_label: "{{ enabled_apps.autoheal_management.ansible_vars.autoheal_container_label }}"
    autoheal_interval_seconds: "{{ enabled_apps.autoheal_management.ansible_vars.autoheal_interval_seconds }}"
    autoheal_start_period_seconds: "{{ enabled_apps.autoheal_management.ansible_vars.autoheal_start_period_seconds }}"
    autoheal_timezone: "{{ enabled_apps.autoheal_management.ansible_vars.autoheal_timezone }}"
  when: enabled_apps.autoheal_management is defined and enabled_apps.autoheal_management is not none
  tags: [host_services, autoheal, autoheal_management]
