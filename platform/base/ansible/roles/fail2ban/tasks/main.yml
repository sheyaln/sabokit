---
- name: Install Fail2ban
  ansible.builtin.apt:
    name: fail2ban
    state: present
    update_cache: true

- name: Ensure Fail2ban service is enabled and running
  ansible.builtin.systemd:
    name: fail2ban
    state: started
    enabled: true
