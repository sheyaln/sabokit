# {{ ansible_managed }}
# Tells the Wazuh dashboard plugin where to find the manager API.
hosts:
  - default:
      url: https://wazuh.manager
      port: 55000
      username: wazuh-wui
      password: "${WAZUH_API_PASSWORD}"
      run_as: false
